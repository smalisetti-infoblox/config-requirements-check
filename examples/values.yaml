# Compliant example: redis is enabled, consolidatedHealth is explicitly
# enabled, and hms is enabled too, satisfying the consolidated-health-enabled-toggle requirement.
redis:
  enabled: true

string:
  val: helloworld2

consolidatedHealth:
  enabled: true

hms:
  enabled: true
